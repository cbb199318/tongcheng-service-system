# 同城服务管理系统数据库设计文档

## 1. 文档信息

- 文档名称：同城服务管理系统数据库设计文档
- 文档版本：V1.0
- 编写日期：2026-05-12
- 依据文档：
  - [同城服务管理系统-产品设计文档](/Users/caobingbing/workspace/tongcheng-service-system/同城服务管理系统-产品设计文档.md)
  - [论文3稿.docx](/Users/caobingbing/workspace/tongcheng-service-system/论文3稿.docx)

## 2. 设计说明

### 2.1 设计目标

本数据库设计严格依据论文中的系统范围进行整理。

按论文业务口径，系统围绕普通用户、服务人员、服务供应商、管理员四类角色设计，覆盖服务预约、订单处理、服务沟通、积分运营、商家运营和平台监管等能力。

按论文数据库详细设计口径，当前核心数据结构重点支撑以下三类主要门户业务：

- 用户端：注册登录、服务浏览、预约下单、支付、评价、商家入驻
- 商家端：店铺维护、服务发布、订单处理、评价回复
- 管理端：用户管理、审核管理、内容管理、订单监管、数据统计

### 2.2 设计原则

- 严格围绕论文已明确的功能范围建模
- 优先保证核心业务闭环
- 保持表结构简单，适合毕业设计实现
- 兼顾后续扩展能力

### 2.3 设计范围

本期核心表共 8 张，严格对应论文第四章数据库逻辑结构的主要实体：

1. `user`
2. `merchant`
3. `service`
4. `order_info`
5. `review`
6. `category`
7. `banner`
8. `notice`

说明：
- 论文中概念名称为“订单表”，考虑 `order` 为 SQL 常见关键字，落库建议使用 `order_info`
- 若项目代码已固定使用 `order`，实现时也可继续沿用，但需注意 SQL 转义
- 论文前文提到的积分兑换、服务人员、服务套餐、人员招募、服务沟通等能力，在核心数据库章节中未单列独立逻辑表
- 因此本文档以论文已明确展开的 8 张核心表为主，并对上述能力保留扩展说明

## 3. 实体关系说明

### 3.1 核心关系

- 一个用户可创建多个订单
- 一个用户可申请一个商家主体
- 一个商家可发布多个服务
- 一个分类下可包含多个服务
- 一个服务可被多个用户下单
- 一个订单关联一个用户、一个商家、一个服务
- 一个订单可对应一条评价

说明：
- 论文中的服务人员、积分运营等能力未在核心数据库章节单列实体
- 若后续将服务人员独立化或积分系统实体化，应在现有核心关系之外补充专门模型

### 3.2 关系示意

```text
user 1 --- n order_info
user 1 --- 1 merchant
merchant 1 --- n service
merchant 1 --- n order_info
service 1 --- n order_info
service n --- 1 category
order_info 1 --- 0..1 review
merchant 1 --- n review
user 1 --- n review
```

## 4. 字段状态约定

### 4.1 用户角色

| 值 | 含义 |
| --- | --- |
| user | 普通用户 |
| staff | 服务人员预留标识 |
| merchant | 商家 |
| admin | 管理员 |

说明：
- 论文业务角色包含服务人员
- 论文核心数据库章节未单列服务人员实体，当前 `staff` 作为业务角色预留值说明，是否单独落库可按实现复杂度决定

### 4.2 通用启用状态

| 值 | 含义 |
| --- | --- |
| 1 | 启用/正常 |
| 0 | 禁用/停用 |

### 4.3 审核状态

| 值 | 含义 |
| --- | --- |
| 0 | 待审核 |
| 1 | 已驳回 |
| 2 | 已通过 |

### 4.4 订单状态

| 值 | 含义 |
| --- | --- |
| 0 | 待支付 |
| 1 | 待接单 |
| 2 | 已接单 |
| 3 | 服务中 |
| 4 | 已完成 |
| 5 | 已取消 |

## 5. 表结构设计

## 5.1 用户表 `user`

### 5.1.1 业务说明

存储平台注册用户、商家账号、管理员账号的基础信息，并为服务人员角色扩展保留统一账号基础。

### 5.1.2 表结构

| 字段名 | 类型 | 长度 | 主键 | 非空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| id | bigint | 20 | 是 | 是 | 自增 | 用户主键 |
| username | varchar | 50 | 否 | 是 | 无 | 登录账号 |
| password | varchar | 100 | 否 | 是 | 无 | 加密密码 |
| nickname | varchar | 50 | 否 | 否 | NULL | 用户昵称 |
| phone | varchar | 20 | 否 | 否 | NULL | 手机号 |
| avatar | varchar | 255 | 否 | 否 | NULL | 头像地址 |
| role | varchar | 20 | 否 | 是 | user | 角色，预留 `staff` |
| status | tinyint | 1 | 否 | 是 | 1 | 账号状态 |
| create_time | datetime | - | 否 | 是 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | datetime | - | 否 | 否 | NULL | 更新时间 |

### 5.1.3 索引设计

| 索引名 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| pk_user_id | id | 主键 | 主键索引 |
| uk_user_username | username | 唯一 | 用户名唯一 |
| idx_user_role | role | 普通 | 角色查询 |
| idx_user_status | status | 普通 | 状态筛选 |

## 5.2 商家表 `merchant`

### 5.2.1 业务说明

存储商家入驻申请及商家主体信息。

### 5.2.2 表结构

| 字段名 | 类型 | 长度 | 主键 | 非空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| id | bigint | 20 | 是 | 是 | 自增 | 商家主键 |
| user_id | bigint | 20 | 否 | 是 | 无 | 关联用户 ID |
| name | varchar | 100 | 否 | 是 | 无 | 商家名称 |
| logo | varchar | 255 | 否 | 否 | NULL | 商家 Logo |
| phone | varchar | 20 | 否 | 是 | 无 | 联系电话 |
| address | varchar | 255 | 否 | 是 | 无 | 商家地址 |
| intro | text | - | 否 | 否 | NULL | 商家简介 |
| audit_status | char | 1 | 否 | 是 | 0 | 审核状态 |
| audit_remark | varchar | 255 | 否 | 否 | NULL | 审核备注 |
| rating | decimal | 3,2 | 否 | 否 | 5.00 | 平均评分 |
| order_count | int | 11 | 否 | 否 | 0 | 订单数量 |
| license | varchar | 255 | 否 | 否 | NULL | 资质证书地址 |
| status | tinyint | 1 | 否 | 是 | 1 | 商家状态 |
| create_time | datetime | - | 否 | 是 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | datetime | - | 否 | 否 | NULL | 更新时间 |

### 5.2.3 索引设计

| 索引名 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| pk_merchant_id | id | 主键 | 主键索引 |
| uk_merchant_user_id | user_id | 唯一 | 一个用户对应一个商家主体 |
| idx_merchant_audit_status | audit_status | 普通 | 审核列表查询 |
| idx_merchant_status | status | 普通 | 商家状态筛选 |
| idx_merchant_name | name | 普通 | 商家搜索 |

## 5.3 服务表 `service`

### 5.3.1 业务说明

存储商家发布的服务项目信息。

### 5.3.2 表结构

| 字段名 | 类型 | 长度 | 主键 | 非空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| id | bigint | 20 | 是 | 是 | 自增 | 服务主键 |
| merchant_id | bigint | 20 | 否 | 是 | 无 | 所属商家 ID |
| category_id | bigint | 20 | 否 | 是 | 无 | 所属分类 ID |
| name | varchar | 100 | 否 | 是 | 无 | 服务名称 |
| price | decimal | 10,2 | 否 | 是 | 0.00 | 服务价格 |
| duration | int | 11 | 否 | 否 | NULL | 预计时长，单位分钟 |
| description | text | - | 否 | 否 | NULL | 服务描述 |
| images | text | - | 否 | 否 | NULL | 服务图片，多图逗号分隔 |
| tags | varchar | 255 | 否 | 否 | NULL | 服务标签 |
| sales | int | 11 | 否 | 否 | 0 | 销量 |
| status | tinyint | 1 | 否 | 是 | 1 | 服务状态 |
| audit_status | char | 1 | 否 | 是 | 0 | 审核状态 |
| audit_remark | varchar | 255 | 否 | 否 | NULL | 审核备注 |
| create_time | datetime | - | 否 | 是 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | datetime | - | 否 | 否 | NULL | 更新时间 |

### 5.3.3 索引设计

| 索引名 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| pk_service_id | id | 主键 | 主键索引 |
| idx_service_merchant_id | merchant_id | 普通 | 商家服务查询 |
| idx_service_category_id | category_id | 普通 | 分类筛选 |
| idx_service_audit_status | audit_status | 普通 | 审核列表查询 |
| idx_service_status | status | 普通 | 上下架筛选 |
| idx_service_name | name | 普通 | 关键字搜索 |

## 5.4 订单表 `order_info`

### 5.4.1 业务说明

存储用户预约下单、支付、接单、完成、取消等全过程数据。

### 5.4.2 表结构

| 字段名 | 类型 | 长度 | 主键 | 非空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| id | bigint | 20 | 是 | 是 | 自增 | 订单主键 |
| order_no | varchar | 50 | 否 | 是 | 无 | 订单编号 |
| user_id | bigint | 20 | 否 | 是 | 无 | 下单用户 ID |
| merchant_id | bigint | 20 | 否 | 是 | 无 | 商家 ID |
| service_id | bigint | 20 | 否 | 是 | 无 | 服务 ID |
| status | char | 1 | 否 | 是 | 0 | 订单状态 |
| address | varchar | 255 | 否 | 是 | 无 | 服务地址 |
| appoint_date | date | - | 否 | 否 | NULL | 预约日期 |
| appoint_slot | varchar | 50 | 否 | 否 | NULL | 预约时段 |
| remark | varchar | 255 | 否 | 否 | NULL | 订单备注 |
| total_price | decimal | 10,2 | 否 | 是 | 0.00 | 订单金额 |
| pay_time | datetime | - | 否 | 否 | NULL | 支付时间 |
| accept_time | datetime | - | 否 | 否 | NULL | 接单时间 |
| start_time | datetime | - | 否 | 否 | NULL | 开始服务时间 |
| complete_time | datetime | - | 否 | 否 | NULL | 完成时间 |
| cancel_time | datetime | - | 否 | 否 | NULL | 取消时间 |
| cancel_reason | varchar | 255 | 否 | 否 | NULL | 取消原因 |
| is_commented | tinyint | 1 | 否 | 是 | 0 | 是否已评价 |
| create_time | datetime | - | 否 | 是 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | datetime | - | 否 | 否 | NULL | 更新时间 |

### 5.4.3 索引设计

| 索引名 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| pk_order_id | id | 主键 | 主键索引 |
| uk_order_no | order_no | 唯一 | 订单编号唯一 |
| idx_order_user_id | user_id | 普通 | 用户订单查询 |
| idx_order_merchant_id | merchant_id | 普通 | 商家订单查询 |
| idx_order_service_id | service_id | 普通 | 服务关联查询 |
| idx_order_status | status | 普通 | 状态筛选 |
| idx_order_create_time | create_time | 普通 | 时间倒序查询 |

## 5.5 评价表 `review`

### 5.5.1 业务说明

存储用户对订单服务的评价及商家回复信息。

### 5.5.2 表结构

| 字段名 | 类型 | 长度 | 主键 | 非空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| id | bigint | 20 | 是 | 是 | 自增 | 评价主键 |
| order_id | bigint | 20 | 否 | 是 | 无 | 关联订单 ID |
| user_id | bigint | 20 | 否 | 是 | 无 | 评价用户 ID |
| merchant_id | bigint | 20 | 否 | 是 | 无 | 商家 ID |
| service_id | bigint | 20 | 否 | 是 | 无 | 服务 ID |
| rating | tinyint | 1 | 否 | 是 | 无 | 评分，1-5 |
| content | varchar | 1000 | 否 | 否 | NULL | 评价内容 |
| images | text | - | 否 | 否 | NULL | 评价图片，多图逗号分隔 |
| reply | varchar | 1000 | 否 | 否 | NULL | 商家回复内容 |
| reply_time | datetime | - | 否 | 否 | NULL | 回复时间 |
| create_time | datetime | - | 否 | 是 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | datetime | - | 否 | 否 | NULL | 更新时间 |

### 5.5.3 索引设计

| 索引名 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| pk_review_id | id | 主键 | 主键索引 |
| uk_review_order_id | order_id | 唯一 | 一个订单只允许一条评价 |
| idx_review_user_id | user_id | 普通 | 用户评价查询 |
| idx_review_merchant_id | merchant_id | 普通 | 商家评价管理 |
| idx_review_service_id | service_id | 普通 | 服务详情展示评价 |
| idx_review_rating | rating | 普通 | 评分统计 |

## 5.6 分类表 `category`

### 5.6.1 业务说明

存储服务分类信息，用于用户端展示和后台维护。

### 5.6.2 表结构

| 字段名 | 类型 | 长度 | 主键 | 非空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| id | bigint | 20 | 是 | 是 | 自增 | 分类主键 |
| name | varchar | 50 | 否 | 是 | 无 | 分类名称 |
| icon | varchar | 255 | 否 | 否 | NULL | 图标地址 |
| sort | int | 11 | 否 | 是 | 0 | 排序值 |
| status | tinyint | 1 | 否 | 是 | 1 | 分类状态 |
| create_time | datetime | - | 否 | 是 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | datetime | - | 否 | 否 | NULL | 更新时间 |

### 5.6.3 索引设计

| 索引名 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| pk_category_id | id | 主键 | 主键索引 |
| uk_category_name | name | 唯一 | 分类名称唯一 |
| idx_category_status | status | 普通 | 启用状态筛选 |
| idx_category_sort | sort | 普通 | 排序查询 |

## 5.7 轮播图表 `banner`

### 5.7.1 业务说明

存储首页 Banner 轮播内容。

### 5.7.2 表结构

| 字段名 | 类型 | 长度 | 主键 | 非空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| id | bigint | 20 | 是 | 是 | 自增 | Banner 主键 |
| title | varchar | 100 | 否 | 否 | NULL | Banner 标题 |
| image_url | varchar | 255 | 否 | 是 | 无 | 图片地址 |
| link_url | varchar | 255 | 否 | 否 | NULL | 跳转链接 |
| sort | int | 11 | 否 | 是 | 0 | 排序值 |
| status | tinyint | 1 | 否 | 是 | 1 | 启用状态 |
| create_time | datetime | - | 否 | 是 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | datetime | - | 否 | 否 | NULL | 更新时间 |

### 5.7.3 索引设计

| 索引名 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| pk_banner_id | id | 主键 | 主键索引 |
| idx_banner_status | status | 普通 | 启用状态筛选 |
| idx_banner_sort | sort | 普通 | 首页排序 |

## 5.8 公告表 `notice`

### 5.8.1 业务说明

存储平台公告信息。

### 5.8.2 表结构

| 字段名 | 类型 | 长度 | 主键 | 非空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| id | bigint | 20 | 是 | 是 | 自增 | 公告主键 |
| title | varchar | 100 | 否 | 是 | 无 | 公告标题 |
| content | text | - | 否 | 是 | 无 | 公告正文 |
| status | tinyint | 1 | 否 | 是 | 1 | 状态 |
| create_time | datetime | - | 否 | 是 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | datetime | - | 否 | 否 | NULL | 更新时间 |

### 5.8.3 索引设计

| 索引名 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| pk_notice_id | id | 主键 | 主键索引 |
| idx_notice_status | status | 普通 | 状态筛选 |
| idx_notice_create_time | create_time | 普通 | 倒序展示 |

## 6. 外键关系建议

毕业设计项目中可根据开发复杂度选择是否真正建立数据库外键约束。

### 6.1 推荐逻辑外键

| 当前表 | 字段 | 关联表 | 关联字段 |
| --- | --- | --- | --- |
| merchant | user_id | user | id |
| service | merchant_id | merchant | id |
| service | category_id | category | id |
| order_info | user_id | user | id |
| order_info | merchant_id | merchant | id |
| order_info | service_id | service | id |
| review | order_id | order_info | id |
| review | user_id | user | id |
| review | merchant_id | merchant | id |
| review | service_id | service | id |

### 6.2 实施建议

- 开发阶段可先不强制建物理外键，避免影响联调效率
- 在代码层完成完整的数据合法性校验
- 正式答辩版可通过 E-R 图和逻辑外键体现数据关系

## 7. 建表 SQL 参考

```sql
CREATE TABLE user (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户主键',
  username VARCHAR(50) NOT NULL COMMENT '登录账号',
  password VARCHAR(100) NOT NULL COMMENT '加密密码',
  nickname VARCHAR(50) DEFAULT NULL COMMENT '用户昵称',
  phone VARCHAR(20) DEFAULT NULL COMMENT '手机号',
  avatar VARCHAR(255) DEFAULT NULL COMMENT '头像地址',
  role VARCHAR(20) NOT NULL DEFAULT 'user' COMMENT '角色：user普通用户 staff服务人员 merchant商家 admin管理员',
  status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1正常 0禁用',
  create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time DATETIME DEFAULT NULL COMMENT '更新时间',
  UNIQUE KEY uk_user_username (username),
  KEY idx_user_role (role),
  KEY idx_user_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

CREATE TABLE merchant (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '商家主键',
  user_id BIGINT NOT NULL COMMENT '关联用户ID',
  name VARCHAR(100) NOT NULL COMMENT '商家名称',
  logo VARCHAR(255) DEFAULT NULL COMMENT '商家Logo',
  phone VARCHAR(20) NOT NULL COMMENT '联系电话',
  address VARCHAR(255) NOT NULL COMMENT '商家地址',
  intro TEXT COMMENT '商家简介',
  audit_status CHAR(1) NOT NULL DEFAULT '0' COMMENT '审核状态：0待审 1驳回 2通过',
  audit_remark VARCHAR(255) DEFAULT NULL COMMENT '审核备注',
  rating DECIMAL(3,2) DEFAULT 5.00 COMMENT '平均评分',
  order_count INT DEFAULT 0 COMMENT '订单数',
  license VARCHAR(255) DEFAULT NULL COMMENT '资质证书',
  status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1正常 0停用',
  create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time DATETIME DEFAULT NULL COMMENT '更新时间',
  UNIQUE KEY uk_merchant_user_id (user_id),
  KEY idx_merchant_audit_status (audit_status),
  KEY idx_merchant_status (status),
  KEY idx_merchant_name (name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商家表';

CREATE TABLE category (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '分类主键',
  name VARCHAR(50) NOT NULL COMMENT '分类名称',
  icon VARCHAR(255) DEFAULT NULL COMMENT '图标地址',
  sort INT NOT NULL DEFAULT 0 COMMENT '排序值',
  status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1启用 0停用',
  create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time DATETIME DEFAULT NULL COMMENT '更新时间',
  UNIQUE KEY uk_category_name (name),
  KEY idx_category_status (status),
  KEY idx_category_sort (sort)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='分类表';

CREATE TABLE service (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '服务主键',
  merchant_id BIGINT NOT NULL COMMENT '商家ID',
  category_id BIGINT NOT NULL COMMENT '分类ID',
  name VARCHAR(100) NOT NULL COMMENT '服务名称',
  price DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '服务价格',
  duration INT DEFAULT NULL COMMENT '预计时长(分钟)',
  description TEXT COMMENT '服务描述',
  images TEXT COMMENT '服务图片',
  tags VARCHAR(255) DEFAULT NULL COMMENT '服务标签',
  sales INT DEFAULT 0 COMMENT '销量',
  status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1启用 0停用',
  audit_status CHAR(1) NOT NULL DEFAULT '0' COMMENT '审核状态：0待审 1驳回 2通过',
  audit_remark VARCHAR(255) DEFAULT NULL COMMENT '审核备注',
  create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time DATETIME DEFAULT NULL COMMENT '更新时间',
  KEY idx_service_merchant_id (merchant_id),
  KEY idx_service_category_id (category_id),
  KEY idx_service_audit_status (audit_status),
  KEY idx_service_status (status),
  KEY idx_service_name (name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='服务表';

CREATE TABLE order_info (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '订单主键',
  order_no VARCHAR(50) NOT NULL COMMENT '订单编号',
  user_id BIGINT NOT NULL COMMENT '用户ID',
  merchant_id BIGINT NOT NULL COMMENT '商家ID',
  service_id BIGINT NOT NULL COMMENT '服务ID',
  status CHAR(1) NOT NULL DEFAULT '0' COMMENT '订单状态',
  address VARCHAR(255) NOT NULL COMMENT '服务地址',
  appoint_date DATE DEFAULT NULL COMMENT '预约日期',
  appoint_slot VARCHAR(50) DEFAULT NULL COMMENT '预约时段',
  remark VARCHAR(255) DEFAULT NULL COMMENT '备注',
  total_price DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '订单金额',
  pay_time DATETIME DEFAULT NULL COMMENT '支付时间',
  accept_time DATETIME DEFAULT NULL COMMENT '接单时间',
  start_time DATETIME DEFAULT NULL COMMENT '开始服务时间',
  complete_time DATETIME DEFAULT NULL COMMENT '完成时间',
  cancel_time DATETIME DEFAULT NULL COMMENT '取消时间',
  cancel_reason VARCHAR(255) DEFAULT NULL COMMENT '取消原因',
  is_commented TINYINT NOT NULL DEFAULT 0 COMMENT '是否已评价',
  create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time DATETIME DEFAULT NULL COMMENT '更新时间',
  UNIQUE KEY uk_order_no (order_no),
  KEY idx_order_user_id (user_id),
  KEY idx_order_merchant_id (merchant_id),
  KEY idx_order_service_id (service_id),
  KEY idx_order_status (status),
  KEY idx_order_create_time (create_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单表';

CREATE TABLE review (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '评价主键',
  order_id BIGINT NOT NULL COMMENT '订单ID',
  user_id BIGINT NOT NULL COMMENT '用户ID',
  merchant_id BIGINT NOT NULL COMMENT '商家ID',
  service_id BIGINT NOT NULL COMMENT '服务ID',
  rating TINYINT NOT NULL COMMENT '评分',
  content VARCHAR(1000) DEFAULT NULL COMMENT '评价内容',
  images TEXT COMMENT '评价图片',
  reply VARCHAR(1000) DEFAULT NULL COMMENT '商家回复',
  reply_time DATETIME DEFAULT NULL COMMENT '回复时间',
  create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time DATETIME DEFAULT NULL COMMENT '更新时间',
  UNIQUE KEY uk_review_order_id (order_id),
  KEY idx_review_user_id (user_id),
  KEY idx_review_merchant_id (merchant_id),
  KEY idx_review_service_id (service_id),
  KEY idx_review_rating (rating)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评价表';

CREATE TABLE banner (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '轮播图主键',
  title VARCHAR(100) DEFAULT NULL COMMENT '标题',
  image_url VARCHAR(255) NOT NULL COMMENT '图片地址',
  link_url VARCHAR(255) DEFAULT NULL COMMENT '跳转地址',
  sort INT NOT NULL DEFAULT 0 COMMENT '排序值',
  status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1启用 0停用',
  create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time DATETIME DEFAULT NULL COMMENT '更新时间',
  KEY idx_banner_status (status),
  KEY idx_banner_sort (sort)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='轮播图表';

CREATE TABLE notice (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '公告主键',
  title VARCHAR(100) NOT NULL COMMENT '公告标题',
  content TEXT NOT NULL COMMENT '公告内容',
  status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1启用 0停用',
  create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time DATETIME DEFAULT NULL COMMENT '更新时间',
  KEY idx_notice_status (status),
  KEY idx_notice_create_time (create_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='公告表';
```

## 8. 初始化数据建议

### 8.1 用户初始化

- 1 个管理员账号
- 2 到 3 个普通用户账号
- 2 个商家账号

### 8.2 分类初始化

建议初始化以下服务分类：

- 家政服务
- 家电维修
- 搬家服务
- 保洁服务
- 跑腿配送
- 教育培训

### 8.3 内容初始化

- 首页轮播图 3 到 5 条
- 平台公告 2 到 3 条
- 每个商家初始化 3 到 5 条服务

## 9. 统计口径说明

### 9.1 今日订单量

- 统计当日创建订单数

### 9.2 今日成交金额

- 统计当日支付成功订单金额汇总

### 9.3 注册用户数

- 统计 `user` 表总记录数

### 9.4 待审核数

- 统计 `merchant.audit_status = 0` 数量
- 可扩展统计 `service.audit_status = 0` 数量

说明：
- 论文数据统计章节重点展示待审核数这一综合指标
- 若需更贴近论文管理视角，可在页面层同时展示待审核商家数与待审核服务数

## 10. 风险与取舍

### 10.1 严格按论文的简化处理

为保证与论文核心数据库章节一致，以下字段采用简化存储方式：

- 服务图片：逗号分隔字符串
- 评价图片：逗号分隔字符串
- 商家资质：单字段存储

说明：
- 积分、沟通记录、人员招募、服务套餐等能力虽在论文前文被提及，但未在数据库核心章节形成统一表结构
- 当前文档不凭空补充核心表，避免与论文第四章逻辑结构发生偏离

### 10.2 后续可扩展方向

若后续项目升级，并在不改变论文核心 8 表主结构的前提下深化实现，可扩展为：

- 图片独立附件表
- 商家员工表
- 服务沟通记录表
- 服务套餐表
- 人员招募表
- 支付流水表
- 订单状态日志表
- 用户积分表
- 消息通知表

## 11. 结论

该数据库设计能够支撑论文中定义的同城服务管理系统核心业务闭环，满足用户下单、商家接单、管理员审核监管的主要场景。对毕业设计项目而言，这套表结构既足够完整，又便于开发和演示，可直接作为数据库建模和后端开发依据。
